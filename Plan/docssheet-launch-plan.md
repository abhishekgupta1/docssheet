# docssheet.com — Launch Plan

Companion to `docssheet-launch-spec.md`. Ordered, actionable steps.

**Scope note (updated 2026-09-09):** The site has been **imported into this
repo** (`github.com/abhishekgupta1/docssheet`) at a **shortlist scope** — 15
flagship guides + 8 cheat sheets + 2 landing pages + 6 info pages — on branch
`import-docssheet-site` (commits: "Add launch planning docs", "Import docssheet
site (shortlist scope)"). The source site at `../abhishekgupta1.github.io` is
now left alone (future personal portfolio). Content selection lives in
`adsense-content-shortlist.md`; the volume tradeoff is flagged in the spec.

---

## Phase 1 — Site import ✅ DONE (branch `import-docssheet-site`)

- ✅ Docusaurus scaffold copied: config, sidebars (autogenerated), `src/css`,
  theme (`Root.js` JSON-LD, trimmed `DocItem/Content` breadcrumb swizzle,
  `MDXComponents` reduced to `AdSlot`), `ConsentBanner` + `AdSlot`,
  `src/data/site.js`, `plugins/og-image`, PWA, local search, `static/`
  (`CNAME` = `docssheet.com`, `robots.txt`, `ads.txt` placeholder, icons).
- ✅ Content: 15 guides + 8 cheat sheets + `docs/intro.md` + `cheatsheets/intro.md`
  + `about`/`contact`/`privacy`/`terms`/`cookie-policy`/`disclaimer`.
- ✅ `projectName` → `docssheet`; `GITHUB_REPO`/`editUrl` → `abhishekgupta1/docssheet`.
- ✅ Navbar/footer: Learn dropdown removed (roadmap/skills/quiz/dashboard/start
  pages not imported). `sidebars.js`: MBA/AI categories removed.
- ✅ Both landing pages rebranded to docssheet; `docs/intro.md` SVG redrawn to
  the three shipped tracks. (Spec R7.)
- ✅ Cross-links to non-imported pages de-linked (`tooling-landscape` guide,
  `privacy.md`, `cookie-policy.md`).
- ✅ `npm run build` clean, **zero broken-link warnings**; `onBrokenLinks:
  'throw'` set. (Spec R22, decision D4.)
- ✅ Front-matter `title`/`description` present on all 23 pages + landings; WIP
  marker scan clean. (Spec R12/R18.)

Remaining Phase 1 work:

1. **Fix the Search Console meta.** `docusaurus.config.js` → `themeConfig.metadata`
   still ships `google-site-verification: REPLACE_WITH_SEARCH_CONSOLE_TOKEN`.
   Paste the real token (after Phase 5) or delete the entry. (Spec R14, D2.)

2. **Per-page QA.** Work through `adsense-content-shortlist.md` §4 for each
   shortlisted page — mobile-clean, code blocks intact, no dead external links,
   nothing that trips AdSense content policy.

3. **Serve locally.** `npm run serve` → click through nav, footer, a few docs,
   a few cheat sheets, the homepage redirect, and the 404 page.

4. **Decide on content volume (D8).** 23 pages, or import more guides from
   `../abhishekgupta1.github.io` first as a hedge against a thin-content reject.

## Phase 2 — GitHub repo & Pages settings

5. **Merge `import-docssheet-site` → `main`** (D9). `main` is what the deploy
   workflow (`.github/workflows/deploy.yml`) runs on.
6. Repo **Settings → Pages** for `abhishekgupta1/docssheet`:
   - **Enable Pages** — Source: **GitHub Actions** (not a branch). It is not on
     yet; the deploy workflow is imported but Pages itself must be switched on.
   - "Custom domain" → `docssheet.com` (GitHub reads `static/CNAME` from the
     build output; re-enter and save if blank).
   - Leave "Enforce HTTPS" unchecked until DNS (Phase 3) has propagated and
     GitHub has issued a certificate.

## Phase 3 — DNS configuration (registrar — confirm which; plan assumes GoDaddy)

7. In DNS management for `docssheet.com`:
   - Four **A records** for `@` → GitHub Pages IPs:
     `185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`
   - One **CNAME** for `www` → `abhishekgupta1.github.io` (the user's Pages host
     serves this project repo under the custom domain).
   - Remove any parked-domain A record or forwarding rule.
8. Verify propagation:
   `dig docssheet.com +noall +answer` and `dig www.docssheet.com +noall +answer`.

## Phase 4 — Go live

9. Once DNS resolves, **Settings → Pages → Enforce HTTPS**.
10. Confirm `https://docssheet.com` and `https://www.docssheet.com` both load
    with a valid certificate. (Spec R1–R3.)
11. Run the manual smoke test (Phase 1 step 3) again on the live domain.
    (Spec R23–R24.)

## Phase 5 — Search visibility (recommended, not blocking)

12. Add `docssheet.com` as a **domain property** in Google Search Console,
    verified via a DNS TXT record — or paste the HTML-tag token into the
    `google-site-verification` meta from Phase 1 step 1.
13. Submit `sitemap.xml`.

## Phase 6 — Google AdSense application

14. At https://www.google.com/adsense/, sign up and add `docssheet.com`.
15. **Add the verification tag without turning ads on.** AdSense gives either a
    `<script src="…adsbygoogle.js?client=ca-pub-…">` loader or a
    `<meta name="google-adsense-account" content="ca-pub-…">` tag. Simplest
    here: add it as an unconditional entry in `docusaurus.config.js`
    `headTags` (not inside the `ADS_ENABLED` block), redeploy. Leave
    `ADS_ENABLED = false` — no ad slots render during review.
16. Submit for review. Turnaround: a few days to a few weeks.
17. **Rejection-risk pre-check** (do before submitting):
    - Branding consistency — ✅ done on import.
    - Privacy policy present and complete — ✅ done.
    - Sufficient original content — ⚠️ now ~23 pages, not 200+. See D8; be ready
      to import more guides if a reviewer flags thin content.
    - Thin / placeholder pages a reviewer might hit — mitigated by the
      `adsense-content-shortlist.md` §4 QA pass.

## Phase 7 — After approval

18. Google issues `pub-XXXXXXXXXXXXXXXX`. Then, in one change:
    - `src/data/site.js`: `ADS_ENABLED = true`, `ADSENSE_CLIENT =
      'ca-pub-XXXXXXXXXXXXXXXX'`.
    - `static/ads.txt`: replace the comment block with
      `google.com, pub-XXXXXXXXXXXXXXXX, DIRECT, f08c47fec0942fa0`.
    - Swap the hand-rolled `ConsentBanner` for a Google-certified CMP / Funding
      Choices for EEA/UK.
19. Add ad units / Auto ads, redeploy, confirm `ads.txt` validates in AdSense.

---

## Open items not owned by this repo
- DNS records at the registrar (Phase 3) — confirm the registrar first; the plan
  assumes GoDaddy.
- GitHub Pages: enable Pages for `abhishekgupta1/docssheet` + custom-domain +
  Enforce HTTPS toggles (Phase 2, 4).
- AdSense and Search Console account setup (Phases 5–6).

## Open decisions to resolve (spec §2.6, D1–D9)
Record an answer for each before submitting the AdSense application:
- **D1** Registrar + DNS live (`dig`); **enable GitHub Pages for the `docssheet`
  repo** (Source: GitHub Actions). Gates Phase 2–4.
- **D2** Create a Search Console domain property for a real token, **or** delete
  the `google-site-verification` meta entry. Before Phase 6. (Phase 1 step 1 / Phase 5.)
- **D3** Confirm a Google AdSense account exists; pick the verification-tag form.
  Real `ca-pub-…` ID required — the `ca-pub-0000000000000000` placeholder will
  not verify. (Phase 6 step 15.)
- **D4** ✅ Resolved — `onBrokenLinks: 'throw'` set, build clean.
- **D5** When to swap `ConsentBanner` for a certified CMP / Funding Choices
  (EEA/UK) — pinned to post-approval, unscheduled. (Phase 7 step 18.)
- **D6** GA4 property and/or `GOATCOUNTER_CODE` at launch, or stay dark.
- **D7** Who owns the AdSense submission, and by when.
- **D8** 23 pages, or import more guides from `../abhishekgupta1.github.io`
  first? Hedge against a thin-content rejection. (Phase 1 step 4 / Phase 6 step 17.)
- **D9** When to merge `import-docssheet-site` → `main` (also triggers deploy). (Phase 2 step 5.)
